import Logi_is
if __name__ is '__main__':
      tk = ''#填写_key区域
      I = #填写账号ID区域
      L = Logi_is.Fis(token = tk)#初始化填写token
      L. Login_HI(li_ = L.Detail_li)
      L.Acc_status(ID_ = I))#获取账号状态
